@model IEnumerable<ProyectoFinalL2.Models.Citas>

@{
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    ViewBag.Title = "Citas";
}

<h2 style="color:white">CITAS</h2>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Clientes.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mascotas.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaReserva)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Clientes.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mascotas.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaReserva)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.CitasID }) |
                @Html.ActionLink("Details", "Details", new { id = item.CitasID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.CitasID })
            </td>
        </tr>
    }

</table>
<br>
<br>
<br>
<p>
    <a href="~/Citas/Create" class="primary-btn header-btn text-uppercase">Crear uno nuevo</a>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
